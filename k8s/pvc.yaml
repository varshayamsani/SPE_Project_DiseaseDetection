apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: disease-detector-data
  namespace: disease-detector
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard  # Adjust based on your cluster


